[facts]
workspace_layout = "Crates, modules, and responsibility boundaries"
quality_commands = "Format, lint, test, and doc commands (e.g., cargo fmt --check)"
error_patterns = "Error types: thiserror vs anyhow, custom enums, diagnostic style"
release_notes = "User-facing changes queued for next release"
msrv_policy = "Minimum supported Rust version and bump policy"
dependency_policy = "When to add deps, preferred crates, audit requirements"
unsafe_invariants = "Documented safety requirements for unsafe blocks"
performance_budget = "Acceptable latency/throughput, profiling tools used"
security_surface = "Crypto, parsing, network, and privilege boundaries"

[events]
review_finding = "Confirmed code review finding with file reference"
release_decision = "Accepted release or versioning decision with semver rationale"
dependency_added = "New crate added with justification and audit status"
unsafe_introduced = "Unsafe block added with safety invariant documented"
msrv_bump = "MSRV increased with changelog entry and breaking notice"
security_advisory = "Known CVE or audit finding tracked"
performance_regression = "Benchmark degradation captured and investigated"
